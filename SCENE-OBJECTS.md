# 場景物件與角色活動 · v0.12.0

所有 32 個空間沿用 2D 視角與原場景圖。共 476 個互動紀錄（103 個既有入口／服務、373 個新觀察物件或群組），其中 444 個有獨立顯示圖層、30 個有開關或外觀狀態。成排座椅、整組燈具、書架上的書和櫃上的杯瓶可共用一個觀察反應，不逐本書建立玩法。

## 資料與呈現

- `src/pixel/scene-object-catalog.js`：逐張檢視現有 12 張場景原圖後標記的來源像素座標、名稱與反應類型。地板不是觀察熱區；門口與正式服務保留原 action。
- `src/pixel/scene-objects.js`：將既有家具與新物品組成統一 registry。每個物件有穩定 ID、hit、target、collision、response、layer 和選填 appearance。`room.entities` 是碰撞的單一來源；原 `blocks` 為其衍生視圖，既有尋路仍可相容讀取。碰撞只使用原本經檢查的地面占用範圍，不拿視覺外框當障礙物。
- `src/pixel/furniture-layers.js`：以原圖裁切與透明遮罩在記憶體拆出背景及獨立 sprite，不重畫整個城市。衣櫃內側與百葉簾為程式繪製的 2D 狀態；燈具、螢幕、指示燈和抱枕有局部外觀變化。不可搬動家具，開關不改動地面碰撞。
- `src/pixel/world.js`：只保留當前空間的合成材質；換場先釋放，背景 atlas 仍最多保留三組。原有前景遮擋使用原圖輪廓，避免破壞坐姿和人物深度。

主要家具（衣櫃、床、桌、座椅、課程／工作服務台）仍走現有 action；新觀察不推進模擬時鐘。觀察物件優先選擇較小的命中範圍，讓桌上物品可以與家具分開點選。

## 狀態與回家

`objectStates[sceneId][objectId]` 只保存該物件允許的字串狀態。讀檔採白名單正規化，舊檔補空物件、未知 ID 與不合法值捨棄。外觀切換不寫入人物能力、金錢或日期，沿用五個手動槽、自動存檔與備份。

「回家」在住處、劇情、對話、當日尚未結算的行動、序章與素材切換時停用。可用時會停止自動行程與走位，再載入住處。素材取得成功後才切換 sceneId；網路失敗留在原場景並恢復按鈕。

## 角色移動與物件活動

- `actor-motion.js`：起步加速、接近終點減速、轉角停頓；高倍速拆成短步，碰撞時停止步伐。所有人物的走路影格使用實際移動距離。
- `npc-behavior.js`：依角色偏好、可達距離與近期紀錄選擇物件。物件底緣附近的合法地面格為接近點；不能站在原圖中心。物件預約和接近點占用避免多人搶同一位置。
- `npc-gestures.js`：用現有角色圖集加上小書本、水杯、手機、澆水壺等圖形，呈現閱讀、喝水、照料與檢查。沒有新增 NPC 坐姿圖集，因此自主活動採站姿；玩家原有坐姿照常。
- `world.js`：依序執行停留、走向物件、使用、收尾與再次選擇。角色碰面時嘗試讓路與繞行；NPC 長時間受阻會放棄並重新選擇。交談會選擇可達的一側，並停止 NPC 活動；劇情與工作預約優先於休閒活動。

`npcBehaviors[id]` 保存場景、活動階段、剩餘時間、目標 ID、最近三項活動及選擇次數。讀檔只恢復合法資料；正在使用物件且位置有效時可續做，行走途中則重新決定路線。舊存檔補預設值，腳下太靠近家具時調整到最近安全格。選單暫停活動時鐘，演出活動不抽取核心隨機數、不推進遊戲日、不消耗玩家金錢，也不改變家具的玩家保存狀態。

## 驗證

- `npm run check`：格式、lint、內容交叉驗證、領域測試、五年模擬、世界反應、資產與 PWA 稽核、正式建置。
- `tests/scene-objects.test.mjs`：全城 registry、服務點可達性、舊存檔與不合法資料、無資源副作用、回家條件與小物件命中。
- `tests/e2e/pixel-scene-objects.spec.mjs`：家具操作／重載、滑鼠與觸控觀察、回家、素材失敗重試，涵蓋 Chromium／WebKit／Firefox 的六個桌機、平板、手機配置。
- 既有全城走訪、人物遮擋、換裝、家具使用、劇情與素材失敗案例另做回歸。

瀏覽器自動測試不代表已驗證所有實體手機。物件群組使用共用反應；沒有加入自由移動家具、物理模擬或 3D。

v0.11.0 執行結果：`npm run check` 通過（417 項領域測試）；新增功能六組瀏覽器配置 24 案例通過；桌機全城走訪、坐姿、換裝及劇情等回歸批次 27 案例通過（其中包含新增功能的桌機案例）。

角色活動另由 `tests/actor-behavior.test.mjs` 與 `tests/e2e/pixel-npc-behavior.spec.mjs` 驗證加減速、碰撞、全城可達性、物件預約、存檔正規化、活動輪替、交談中斷、暫停及舊檔邊界恢復。

v0.12.0 執行結果：`npm run check` 通過（423 項領域測試）；桌機全城／家具／劇情／NPC 回歸 30 案例通過。NPC 行為在六組瀏覽器配置共 24 案例驗證完成；首次平行執行有一個 Firefox 素材載入超過 5 秒，改為 15 秒等待後，Firefox 四案重跑全數通過。
