import{test,expect}from"@playwright/test";

test("創角、自動存檔、重新整理與離線啟動",async({page,context})=>{const errors=[];page.on("console",msg=>{if(msg.type()==="error")errors.push(msg.text())});await page.goto("/");await page.locator("#player-name").fill("測試新人");await expect(page.locator("#to-stats")).toBeEnabled();await page.locator("#to-stats").click();await page.locator("#start").click();await expect(page.locator("body")).toContainText("星途未定");const save=await page.evaluate(()=>localStorage.getItem("star-game-save"));expect(save).toContain("測試新人");await page.reload();await expect(page.locator("body")).toContainText("測試新人");await page.reload();await page.waitForFunction(()=>navigator.serviceWorker?.controller||false);await context.setOffline(true);await page.reload();await expect(page.locator("body")).toContainText("星途未定");expect(errors).toEqual([])});
