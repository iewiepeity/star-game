import test from"node:test";
import assert from"node:assert/strict";
import{ABILITIES}from"../src/data/abilities.js";
import{JOB_BY_ID}from"../src/data/jobs-catalog.js";
import{state,resetState,hydrateState}from"../src/core/state.js";
import{setSeed,randomInt}from"../src/core/rng.js";
import{validateGameState}from"../src/core/save-schema.js";
import{ensureJobState,applyForJob,signJob,scheduleJobSession,completeJobSession,checkJobDeadlines}from"../src/logic/job-engine.js";
import{meetNpc,adjustRelationship,relationshipStage,advanceRomance}from"../src/logic/npc-engine.js";
import{matchesConditions,resolveEvent,processQueuedEvents,activateNextEvent,EVENT_KINDS,classifyEvent}from"../src/logic/event-engine.js";
import{addCompletedWork,resolveAwardSeason}from"../src/logic/portfolio.js";
import{evaluateEnding,endingSnapshot}from"../src/logic/career.js";
import{createCreativeProject,submitCreativeProject,startCreativeProduction,releaseCreativeProject}from"../src/logic/creative.js";
import{creativeActionState}from"../src/logic/creative-workflow.js";
import{generateIndustryNews}from"../src/logic/industry-news.js";
import{queueNpcStoryEvents}from"../src/logic/npc-storylines.js";
function fresh(){resetState();state.name="測試玩家";state.stats=Object.fromEntries(ABILITIES.map(name=>[name,1000]));state.hidden={幽默:700,共情:700,洞察:700,膽識:700,品德:700,自律:700,野心:700,抗壓:700};state.schedule=Array(7).fill("rest");state.freeLocations=Array(7).fill(null);state.scheduledJobIds=Array(7).fill(null);state.scheduledActivityIds=Array(7).fill(null);state.trainingSessionsCompleted=100;setSeed("test-seed")}
test("Seed RNG 可重現",()=>{fresh();const a=[randomInt(1,100),randomInt(1,100),randomInt(1,100)];setSeed("test-seed");const b=[randomInt(1,100),randomInt(1,100),randomInt(1,100)];assert.deepEqual(a,b)});
test("通告 domain 會阻擋未達資格的直接申請",()=>{fresh();state.stats.鏡頭感=0;assert.equal(applyForJob("J001"),false);assert.equal(state.activeJobs.J001.stage,"available")});
test("多通告可同時簽約、依指定日排程並建立作品履歷",()=>{fresh();for(const id of["J001","J002"]){const record=ensureJobState(id);record.stage="passed";assert.equal(signJob(id),true);assert.equal(scheduleJobSession(id).ok,true)}assert.equal(Object.values(state.activeJobs).filter(job=>job.stage==="active").length,2);const id=state.scheduledJobIds.find(Boolean),record=state.activeJobs[id];record.remainingSessions=1;const result=completeJobSession(id);assert.equal(result.completed,true);assert.equal(state.completedWorks.length,1)});
test("逾期通告會個別違約，不影響其他合約",()=>{fresh();const a=ensureJobState("J001"),b=ensureJobState("J002");Object.assign(a,{stage:"active",deadlineWeek:1,remainingSessions:2});Object.assign(b,{stage:"active",deadlineWeek:9,remainingSessions:2});state.week=2;assert.deepEqual(checkJobDeadlines(),["J001"]);assert.equal(a.stage,"breached");assert.equal(b.stage,"active")});
test("NPC 引擎集中處理初遇、里程碑事件與戀愛狀態",()=>{fresh();assert.equal(meetNpc("jiqing").met,true);adjustRelationship("jiqing",{closeness:40,trust:30,source:"共同工作"});const rel=state.relationships.jiqing;assert.equal(relationshipStage(rel).id,"friend");assert.ok(state.eventQueue.length>=1);assert.equal(advanceRomance("jiqing","interested"),true);assert.equal(rel.romance,"interested")});
test("事件 DSL 支援複合效果與關係條件",()=>{fresh();meetNpc("jiqing");adjustRelationship("jiqing",{closeness:30,trust:20});const event={title:"複合事件",text:"一次改很多東西",requires:{moneyMin:100,relationship:{jiqing:{closeness:20}}},effects:[{stat:"演技",value:3},{rep:"業界評價",value:4},{fame:2},{flag:"combo"}]};assert.equal(matchesConditions(event.requires),true);resolveEvent(event);assert.equal(state.stats.演技,1000);assert.equal(state.rep.業界評價,4);assert.equal(state.fame,2);assert.ok(state.eventFlags.includes("combo"))});
test("事件 DSL 可建立交惡條件與敵意效果",()=>{fresh();meetNpc("jiqing");resolveEvent({title:"公開衝突",text:"關係惡化",effect:{npc:"jiqing",relation:-4,trust:-8,affection:-5,hostility:48}});assert.equal(matchesConditions({relationship:{jiqing:{hostilityMin:45}}}),true);assert.equal(matchesConditions({relationship:{jiqing:{hostilityMax:20}}}),false);assert.equal(state.relationships.jiqing.stage,"hostile")});
test("延遲事件到期後進入可見事件佇列，不再背景偷偷結算",()=>{fresh();resolveEvent({title:"埋下伏筆",text:"現在還看不出影響",effect:{delayWeeks:2},followUp:{delayWeeks:2,event:{title:"後續回響",text:"過去的選擇回來了",effect:{fame:5}}}});state.week=2;assert.equal(processQueuedEvents(),0);state.week=3;assert.equal(processQueuedEvents(),1);assert.equal(state.fame,0);activateNextEvent();assert.equal(state.activeEvent.event.title,"後續回響");resolveEvent(state.activeEvent.event);assert.equal(state.fame,5)});
test("作品依類型進入真正對應的年度典禮週",()=>{fresh();state.week=30;const job=JOB_BY_ID.J031;const work=addCompletedWork(job,{quality:95});assert.equal(state.awards.length,0);const season=Object.values(state.awardSeasons).find(s=>s.candidates.includes(work.id));assert.ok(season);state.week=season.ceremonyWeek;setSeed("award-test");resolveAwardSeason(season.year,season.category);assert.equal(season.resolved,true)});
test("自創作品採用後必須製作並發行，才進正式作品履歷",()=>{fresh();const p=createCreativeProject("song","測試單曲");Object.assign(p,{progress:100,quality:1000,status:"ready"});state.discoveredCompanies.push("echo_records");state.rep.業界評價=1000;setSeed("creative-accept");let result=submitCreativeProject(p.id,"echo_records");for(let i=0;!result.accepted&&i<5;i++){p.status="ready";result=submitCreativeProject(p.id,"echo_records")}assert.equal(result.accepted,true);assert.equal(state.completedWorks.length,0);while(p.status!=="ready_release")startCreativeProduction(p.id);const released=releaseCreativeProject(p.id);assert.equal(p.status,"released");assert.ok(released.score>0);assert.equal(state.completedWorks.at(-1).original,true)});
test("娛樂圈新聞會接住玩家發行與 NPC 動態",()=>{fresh();state.creativeProjects.push({id:"CP-X",title:"紅了嗎",status:"released",releaseWeek:1,marketScore:88,category:"歌曲",team:[]});const made=generateIndustryNews({npcUpdates:["喬映澄的職涯明顯往上走了一階"]});assert.ok(made.length>=2);assert.ok(state.industryNews.some(n=>n.title.includes("紅了嗎")));assert.ok(state.industryNews.some(n=>n.title.includes("喬映澄")))});
test("NPC 關係階段會產生一次性的可見人物事件",()=>{fresh();meetNpc("jiqing");adjustRelationship("jiqing",{closeness:42,trust:32});state.eventQueue=[];const first=queueNpcStoryEvents();const second=queueNpcStoryEvents();assert.ok(first.length>=1);assert.equal(second.length,0);assert.ok(state.eventQueue.some(x=>x.event.kind==="人物事件"))});
test("舊版單通告存檔只在 hydrate 時遷移，runtime 不再保留舊欄位",()=>{fresh();const legacy=structuredClone(state);delete legacy.activeJobs;legacy.jobStage="active";legacy.jobRemaining=2;legacy.jobDeadline=3;hydrateState(legacy);assert.equal(state.activeJobs.J001.stage,"active");assert.equal("jobStage"in state,false);assert.equal("jobRemaining"in state,false)});
test("結局 snapshot 不會受後續 state 變動污染",()=>{fresh();state.completedWorks=[{quality:90,category:"電影"}];state.careerProgress.電影=900;state.awards=[{result:"得獎"}];state.money=1200000;state.fame=800;state.fans=100000;meetNpc("guchengxi");adjustRelationship("guchengxi",{closeness:80,trust:80});const snap=endingSnapshot("fiveyear");state.money=0;state.endingSnapshot=snap;const result=evaluateEnding("fiveyear");assert.equal(result.snapshot.money,1200000);assert.ok(result.badges.includes("重要羈絆"))});
test("v15 存檔 schema、五槽、匯出匯入與舊版遷移",async()=>{const store=new Map;globalThis.localStorage={getItem:key=>store.get(key)||null,setItem:(key,value)=>store.set(key,value),removeItem:key=>store.delete(key)};const persistence=await import(`../src/core/persistence.js?test=${Date.now()}`);fresh();state.week=12;assert.equal(validateGameState(state).ok,true);assert.equal(persistence.SAVE_VERSION,15);assert.equal(persistence.saveManualSlot(3,state,"第三槽"),true);assert.equal(persistence.loadManualSlot(3).week,12);const exported=persistence.exportSave(state);assert.equal(persistence.parseImportedSave(exported).week,12);assert.equal(persistence.manualSlotMetas().length,5);assert.throws(()=>persistence.parseImportedSave(JSON.stringify({game:"star-game",v:11,state:{week:-1}})))});
test("手動存檔覆蓋與刪除都能復原上一份內容",async()=>{const store=new Map;globalThis.localStorage={getItem:key=>store.get(key)||null,setItem:(key,value)=>store.set(key,value),removeItem:key=>store.delete(key)};const persistence=await import(`../src/core/persistence.js?restore=${Date.now()}`);fresh();state.week=40;assert.equal(persistence.saveManualSlot(2,state,"長期進度"),true);state.week=2;assert.equal(persistence.saveManualSlot(2,state,"新進度"),true);assert.equal(persistence.recentManualMeta().week,40);assert.equal(persistence.restoreRecentManualSlot(),2);assert.equal(persistence.loadManualSlot(2).week,40);assert.equal(persistence.deleteManualSlot(2),true);assert.equal(persistence.loadManualSlot(2),null);assert.equal(persistence.restoreRecentManualSlot(),2);assert.equal(persistence.loadManualSlot(2).week,40)});
test("創作流程狀態集中回傳可操作性與明確原因",()=>{assert.equal(creativeActionState({status:"draft"},"work").ok,true);assert.equal(creativeActionState({status:"draft"},"route").ok,false);assert.match(creativeActionState({status:"draft"},"route").reason,/完成草稿/);assert.equal(creativeActionState({status:"contracted",productionSessions:0},"budget").ok,true);assert.equal(creativeActionState({status:"production",productionSessions:1},"budget").ok,false);assert.match(creativeActionState(null,"release").reason,/不存在/)});
test("新版 App 分類皆可通過驗證且不會污染持久化存檔",async()=>{const persistence=await import(`../src/core/persistence.js?categories=${Date.now()}`);fresh();for(const category of["全部","規劃","事業","人物","世界","紀錄","個人","系統"]){state.appCategory=category;assert.equal(validateGameState(state).ok,true);assert.equal(Object.hasOwn(persistence.persistableState(state),"appCategory"),false)}});
test("事件分類仍維持資料驅動",()=>{fresh();assert.equal(classifyEvent({choices:[{id:"x"}]}),EVENT_KINDS.CHOICE);assert.equal(classifyEvent({effect:{fame:1}}),EVENT_KINDS.PUBLIC)});
