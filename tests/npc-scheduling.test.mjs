import test from"node:test";import assert from"node:assert/strict";import{resetState,state}from"../src/core/state.js";import{reserveNpcJobSchedule,canCastFitJob,isNpcBusy,releaseNpcJobSchedule}from"../src/logic/npc-ecosystem.js";

test("NPC 可以尬戲，但同一週同一天不能被兩份通告重複預約",()=>{resetState();const npc="tangtang";const jobA={id:"TEST-A",workDays:[1],sessions:2,deadlineWeeks:2},recordA={jobId:jobA.id,npcCast:[npc],signedWeek:1,deadlineWeek:2,remainingSessions:2,npcScheduleSlots:[]};state.activeJobs[jobA.id]=recordA;assert.equal(reserveNpcJobSchedule(jobA,recordA).ok,true);assert.equal(isNpcBusy(npc,1,1),true);assert.equal(isNpcBusy(npc,2,1),true);const clash={id:"TEST-B",workDays:[1],sessions:1,deadlineWeeks:2};assert.equal(canCastFitJob([npc],clash,1,2),false);const compatible={id:"TEST-C",workDays:[2],sessions:2,deadlineWeeks:2};assert.equal(canCastFitJob([npc],compatible,1,2),true)});

test("通告結束釋放未使用 NPC 檔期",()=>{resetState();const npc="tangtang",job={id:"TEST-A",workDays:[1],sessions:2,deadlineWeeks:2},record={jobId:job.id,npcCast:[npc],signedWeek:1,deadlineWeek:2,remainingSessions:2,npcScheduleSlots:[]};state.activeJobs[job.id]=record;reserveNpcJobSchedule(job,record);releaseNpcJobSchedule(job.id);assert.equal(isNpcBusy(npc,1,1),false);assert.equal(isNpcBusy(npc,2,1),false)});
