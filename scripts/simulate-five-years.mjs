// 快速平衡壓測：不取代 E2E，而是用固定策略找出五年中後期數值與重複風險。
const ARCHETYPES=["actor","singer","variety","allrounder","romance","career","casual","overwork","creator","controversial"];
const weeks=260;
function run(type){let money=18000,fame=0,fatigue=0,health=100,works=0,relationships=0,training=0,failures=0,scandals=0,creative=0;const seen=new Map();for(let week=1;week<=weeks;week++){const y=Math.floor((week-1)/52)+1;let action="train";if(type==="actor")action=week%3?"acting-job":"train";if(type==="singer")action=week%3?"song-job":"train";if(type==="variety")action=week%2?"variety-job":"social";if(type==="allrounder")action=["acting-job","song-job","train","social"][week%4];if(type==="romance")action=week%2?"social":"rest";if(type==="career")action=week%4?"job":"train";if(type==="casual")action=["rest","free","train","job"][week%4];if(type==="overwork")action="job";if(type==="creator")action=week%3?"creative":"job";if(type==="controversial")action=week%3?"job":"scandal";seen.set(action,(seen.get(action)||0)+1);if(action.includes("job")||action==="job"){works+=week%(7-y)===0?1:0;money+=2400+y*650;fame+=2+y;fatigue+=12}else if(action==="train"){training++;money-=900;fatigue+=7}else if(action==="social"){relationships++;money-=500;fatigue+=3}else if(action==="creative"){creative++;money-=1200;fame+=week%8===0?6:0;fatigue+=8}else if(action==="scandal"){scandals++;fame+=4;fatigue+=5}else{fatigue-=10;health+=2}if(fatigue>80){health-=8;if(type!=="overwork")fatigue-=18}else health=Math.min(100,health+1);if(health<35){failures++;health+=20;fatigue=45;money-=2500}fatigue=Math.max(0,Math.min(100,fatigue))}return{type,money,fame,fatigue,health,works,relationships,training,failures,scandals,creative,dominant:[...seen.entries()].sort((a,b)=>b[1]-a[1])[0]}}
const report=ARCHETYPES.map(run);console.table(report);const warnings=[];for(const r of report){if(r.money<0)warnings.push(`${r.type}: 五年後破產`);if(r.health<25)warnings.push(`${r.type}: 健康失衡`);if(r.works===0&&!["romance","casual"].includes(r.type))warnings.push(`${r.type}: 沒有形成作品`);if(r.dominant?.[1]>210)warnings.push(`${r.type}: 單一行為超過 210 週，可能重複`)}console.log(JSON.stringify({weeks,archetypes:report,warnings},null,2));if(warnings.length>8)process.exitCode=1;
