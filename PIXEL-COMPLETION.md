# 像素版 v0.7.0 · 功能整合

手機對話改為緊湊的整行選項，修正咖啡館「聊近況」到位後無法執行，並補齊 v0.6.0 功能清單中尚未接入的原版操作。

## 從哪裡開始

- 選單 → 隨身手帳：全部功能；可搜尋、分類與編輯六個常用捷徑。
- 手帳 → 人物 → 選擇已認識的人 → 聊近況 → 選日期 → 今日行動：角色會到約定場景互動並結算。
- 手帳 → 創作：完整方向、投稿、團隊分工、預算、自製與發行。
- 存檔 → 旅程搬移：匯出、預覽匯入、原版存檔轉入與備份還原。
- 設定：配色、字級、音量、教學、重新開始、提前退圈與完整離線準備。

詳見 `PIXEL-FEATURE-PARITY.md` 的完整 18 項功能核對表。

## 開發說明

像素入口仍是 `pixel.html`。保留舊像素存檔鍵，不需要重開；原版存檔只在玩家確認後移入。所有資訊頁共用原版資料與規則，任何占用一天的行動都走像素行程與結算。

共享 service worker 版本為 1.30.0。完整發行包須包含 dist 全部內容，不能只複製像素頁，否則共用快取的原版 shell 不完整。離線清單由 build 自動建立，包含人物圖集 JSON、場景、插畫、程式與音效。

## 驗證

- `npm run check` 通過：354 項領域測試、內容檢查、五年模擬、世界反應、資產與 PWA 檢查，以及正式建置。
- 像素瀏覽器案例分批回歸：176 項不同案例通過。涵蓋 Chromium、WebKit、Firefox，以及桌面、手機、平板視窗；全城遍歷與完整離線下載各只在桌面執行，另外 10 個重複組合依測試設定略過。
- 最後變更另驗第一週七天行程與換裝／讀檔，六個瀏覽器尺寸組合共 12 項通過；手帳搜尋、分類、捷徑保存和提前退圈已在六個組合驗證。
- 4194 實際發行包驗證：390 × 844 標準字、360 × 780 大字、1280 × 800 桌面。17 個手帳功能按鈕存在，13 個完整資訊頁無水平溢出，沒有程式例外或 HTTP 資源錯誤。
- 實際斷網後可重新開啟像素入口，再從地圖進入尚未到訪的 Live House；人物圖集 JSON 已納入完整離線清單。

正式互動會受原版資格與進度限制；通過上述測試不代表已窮舉每一段故事或每款實機。
