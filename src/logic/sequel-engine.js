import { titleTag } from "../core/utils.js";
import{state}from"../core/state.js";import{chance,randomInt}from"../core/rng.js";import{enqueueVisibleEvent}from"./event-engine.js";import{scheduleActivity}from"./scheduled-activities.js";import{isNpcBusy,reserveNpcExternalSlot,completeNpcExternalSlot,adjustNpcSocialRelation}from"./npc-ecosystem.js";import{adjustRelationship}from"./npc-engine.js";import{registerAwardCandidate}from"./portfolio.js";
const openDay=day=>["rest","free"].includes(state.schedule?.[day])&&!state.scheduledJobIds?.[day]&&!state.scheduledActivityIds?.[day];
export function sequelOffer(id){return(state.sequelOffers||[]).find(o=>o.id===id)||null}
export function syncSequelOfferFlags(){for(const offer of state.sequelOffers||[]){if(offer.status!=="offered")continue;if(state.eventFlags.includes(`sequel-accepted:${offer.id}`)){offer.status="active";offer.acceptedWeek=state.week;offer.requiredSessions=offer.requiredSessions||Math.max(2,Math.min(4,Math.ceil((offer.stars||3)/1.5)));offer.completedSessions=offer.completedSessions||0}else if(state.eventFlags.includes(`sequel-negotiate:${offer.id}`)){offer.status="active";offer.acceptedWeek=state.week;offer.negotiated=true;offer.payMultiplier=1.25;offer.requiredSessions=offer.requiredSessions||Math.max(2,Math.min(4,Math.ceil((offer.stars||3)/1.5)));offer.completedSessions=offer.completedSessions||0}else if(state.eventFlags.includes(`sequel-declined:${offer.id}`)){offer.status="declined";offer.closedWeek=state.week}}return state.sequelOffers}
export function tickSequelOpportunities(){state.sequelOffers??=[];syncSequelOfferFlags();const candidates=(state.completedWorks||[]).filter(w=>(w.quality||0)>=78&&(w.stars||0)>=3&&!state.sequelOffers.some(o=>o.workId===w.id));const work=candidates.find(()=>chance(18));if(!work)return null;const offer={id:`SEQ-${state.week}-${work.id}`,workId:work.id,title:`${work.title}續作`,week:state.week,status:"offered",npcCast:[...(work.npcCast||[])],category:work.category,stars:work.stars,quality:work.quality,role:work.role,roleId:work.roleId||"featured",tags:[...(work.tags||[]),"續作"],requiredSessions:Math.max(2,Math.min(4,Math.ceil((work.stars||3)/1.5))),completedSessions:0,payMultiplier:1};state.sequelOffers.push(offer);enqueueVisibleEvent({id:`sequel:${offer.id}`,kind:"職涯事件",title:`續作邀請｜${titleTag(work.title)}`,text:"製作方希望延續前作聲量，提出原班人馬回歸的續作邀請。接受代表要預留檔期，拒絕則可能由其他藝人接手。",choices:[{id:"return",label:"接受回歸",outcome:"你優先保留續作合作權，市場對原班人馬的討論開始升溫。",effects:[{flag:`sequel-accepted:${offer.id}`},{rep:"話題度",value:8}]},{id:"negotiate",label:"先談更好的條件",outcome:"製作方接受你提出的加價條件，但也要求照原定製作期完成。",effects:[{flag:`sequel-negotiate:${offer.id}`},{rep:"商業價值",value:5}]},{id:"decline",label:"拒絕續作，嘗試轉型",outcome:"你把檔期留給其他方向，外界開始猜測下一步。",effects:[{flag:`sequel-declined:${offer.id}`},{rep:"業界評價",value:2}]}]},"續作");return offer}
export function scheduleSequelSession(id){syncSequelOfferFlags();const offer=sequelOffer(id);if(!offer||offer.status!=="active")return{ok:false,message:"這份續作目前無法安排。"};const key=`sequel:${offer.id}`,day=[0,1,2,3,4,5,6].find(d=>openDay(d)&&(offer.npcCast||[]).every(n=>!isNpcBusy(n,state.week,d,key)));if(day==null)return{ok:false,message:"本週沒有玩家與原班人馬都能配合的共同檔期。"};const r=scheduleActivity("sequel_session",{offerId:offer.id},`${titleTag(offer.title)}製作`,{fatigue:12,stamina:10,preferredDay:day});if(!r.ok)return r;for(const npcId of offer.npcCast||[])reserveNpcExternalSlot(npcId,{key,week:state.week,day:r.day,label:`${titleTag(offer.title)}續作`});offer.lastScheduledWeek=state.week;return r}
export function completeSequelSession(id){const offer=sequelOffer(id);if(!offer||offer.status!=="active")return{ok:false,text:"續作合約已失效。"};offer.completedSessions+=1;const key=`sequel:${offer.id}`;for(const npcId of offer.npcCast||[]){completeNpcExternalSlot(npcId,key,state.week,state.runnerDay);if(state.knownPeople.includes(npcId))adjustRelationship(npcId,{closeness:2,trust:2,source:`${titleTag(offer.title)}續作合作`})}if((offer.npcCast||[]).length>1)for(let i=0;i<offer.npcCast.length;i++)for(let j=i+1;j<offer.npcCast.length;j++)adjustNpcSocialRelation(offer.npcCast[i],offer.npcCast[j],1,`${titleTag(offer.title)}續作合作`);if(offer.completedSessions<offer.requiredSessions)return{ok:true,completed:false,text:`續作完成第 ${offer.completedSessions}/${offer.requiredSessions} 次製作。`};offer.status="completed";offer.completedWeek=state.week;const original=state.completedWorks.find(w=>w.id===offer.workId),quality=Math.max(55,Math.min(100,(offer.quality||75)+randomInt(-7,8))),pay=Math.round((5000+(offer.stars||3)*4500)*(offer.payMultiplier||1));state.money+=pay;state.fame+=Math.max(3,(offer.stars||3)*3);state.fans+=Math.max(20,(offer.stars||3)*30);const work={id:`sequel-work-${offer.id}`,jobId:null,sequelOfferId:offer.id,parentWorkId:offer.workId,title:offer.title,category:offer.category,tags:[...(offer.tags||[])],stars:offer.stars,role:offer.role||"原班人馬回歸",roleId:offer.roleId||"featured",awardMultiplier:original?.awardMultiplier??1,client:original?.client||"原製作方",quality,completedWeek:state.week,completedYear:Math.ceil(state.week/52),fame:(offer.stars||3)*3,fans:(offer.stars||3)*30,npcCast:[...(offer.npcCast||[])],awards:[],sequel:true};state.completedWorks.push(work);state.careerProgress[offer.category]=(state.careerProgress[offer.category]||0)+work.stars*work.quality;registerAwardCandidate(work);state.flags.push({week:state.week,label:`續作完成：${offer.title}`,note:`原班人馬回歸，作品品質 ${quality}，收入 ${pay}。`});return{ok:true,completed:true,work,pay,text:`${titleTag(offer.title)}完成！品質 ${quality}、收入＋${pay}。`}}
