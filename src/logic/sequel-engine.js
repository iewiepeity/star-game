import{state}from"../core/state.js";import{chance}from"../core/rng.js";import{enqueueVisibleEvent}from"./event-engine.js";
export function tickSequelOpportunities(){state.sequelOffers??=[];const candidates=(state.completedWorks||[]).filter(w=>(w.quality||0)>=78&&(w.stars||0)>=3&&!state.sequelOffers.some(o=>o.workId===w.id));const work=candidates.find(()=>chance(18));if(!work)return null;const offer={id:`SEQ-${state.week}-${work.id}`,workId:work.id,title:`${work.title}續作`,week:state.week,status:"offered",npcCast:[...(work.npcCast||[])]};state.sequelOffers.push(offer);enqueueVisibleEvent({id:`sequel:${offer.id}`,kind:"職涯事件",title:`續作邀請｜《${work.title}》`,text:"製作方希望延續前作聲量，提出原班人馬回歸的續作邀請。接受代表要預留檔期，拒絕則可能由其他藝人接手。",choices:[{id:"return",label:"接受回歸",outcome:"你優先保留續作合作權，市場對原班人馬的討論開始升溫。",effects:[{flag:`sequel-accepted:${offer.id}`},{rep:"話題度",value:8}]},{id:"negotiate",label:"先談更好的條件",outcome:"製作方願意重新評估條件，但也提醒檔期不能拖太久。",effects:[{flag:`sequel-negotiate:${offer.id}`},{rep:"商業價值",value:5}]},{id:"decline",label:"拒絕續作，嘗試轉型",outcome:"你把檔期留給其他方向，外界開始猜測下一步。",effects:[{flag:`sequel-declined:${offer.id}`},{rep:"業界評價",value:2}]}]},"續作");return offer}
