import{state}from"../core/state.js";
import{NPCS}from"../data/npcs.js";
import{relationshipStage}from"./npc-engine.js";
import{enqueueVisibleEvent}from"./event-engine.js";
const STAGE_COPY={acquaintance:{title:"關係開始有了名字",text:n=>`最近幾次碰面後，${n.name}已經不再只是通訊錄裡的一個名字。`,choices:n=>[{id:"warm",label:"主動多聊一點",outcome:`你和${n.name}的距離又近了一些。`,effect:{npc:n.id,relation:4,trust:2}},{id:"steady",label:"照現在的步調就好",outcome:"你沒有刻意加速這段關係。",effect:{mood:1}}]},familiar:{title:"圈內熟人變成真正的熟人",text:n=>`${n.name}開始會在沒有工作理由的時候找你說幾句。`,choices:n=>[{id:"open",label:"也分享自己的近況",outcome:`${n.name}明顯更信任你了。`,effect:{npc:n.id,relation:5,trust:5}},{id:"work",label:"先聊最近的工作",outcome:"你們的默契更偏向可靠的工作夥伴。",effect:{npc:n.id,relation:2,trust:6}}]},friend:{title:"工作之外的邀請",text:n=>`${n.name}傳來一則訊息：這次不是通告，也不是公事。`,choices:n=>[{id:"go",label:"赴約",outcome:"這次見面沒有鏡頭，也沒有誰需要表現。",effect:{npc:n.id,relation:8,trust:5,mood:4}},{id:"later",label:"最近真的太忙了",outcome:`${n.name}說沒關係，下次再約。`,effect:{npc:n.id,relation:1,trust:1}}]},close:{title:"只有熟人才會知道的事",text:n=>`${n.name}第一次主動提起自己很少對外說的壓力。`,choices:n=>[{id:"listen",label:"先聽，不急著給答案",outcome:"你沒有替對方解決問題，但讓這件事變得沒那麼難扛。",effect:{npc:n.id,relation:5,trust:10}},{id:"encourage",label:"認真替對方打氣",outcome:`${n.name}笑著說，至少這句話他會記得。`,effect:{npc:n.id,relation:7,trust:6}}]}};
export function queueNpcStoryEvents(){state.npcStoryHistory??=[];const queued=[];for(const id of state.knownPeople||[]){const npc=NPCS[id],rel=state.relationships[id];if(!npc||!rel)continue;const stage=relationshipStage(rel).id,copy=STAGE_COPY[stage];if(!copy)continue;const key=`${id}:${stage}`;if(state.npcStoryHistory.includes(key))continue;const event={id:`npc-story-${key}`,kind:"人物事件",title:`${npc.name}｜${copy.title}`,text:copy.text(npc),choices:copy.choices(npc)};enqueueVisibleEvent(event,"人物關係");state.npcStoryHistory.push(key);queued.push(event.id)}return queued}
