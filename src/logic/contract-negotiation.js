import{state}from"../core/state.js";import{effectiveStat}from"../core/utils.js";
export function negotiationPower(){const talk=effectiveStat("口才")||0,industry=state.rep?.業界評價||0,commercial=state.rep?.商業價值||0,manager=state.managerState?.trust||50;return Math.round(talk*.35+industry*.25+commercial*.2+manager*2)}
export function negotiateAgencyTerms(agency,request="commission"){if(!agency)return{ok:false,message:"找不到經紀公司資料。"};state.contractNegotiations??=[];const power=negotiationPower(),base=agency.contract||{},chance=Math.max(10,Math.min(90,30+power/20-(state.contractNegotiations.filter(x=>x.agencyId===agency.id).length*8))),roll=(state.rngCursor*37+state.week*17+power)%100,ok=roll<chance;const terms={years:base.years||3,commission:base.commission||25,creativeFreedom:50,guaranteedAuditions:0,renewalPriority:true};if(ok){if(request==="commission")terms.commission=Math.max(10,terms.commission-5);if(request==="term")terms.years=Math.max(1,terms.years-1);if(request==="creative")terms.creativeFreedom=75;if(request==="auditions")terms.guaranteedAuditions=3}const record={week:state.week,agencyId:agency.id,request,ok,power,chance,terms};state.contractNegotiations.push(record);return{ok,chance,terms,message:ok?"公司接受部分條件調整。":"公司認為目前籌碼還不足，維持原合約。"}}
