import{state}from"../core/state.js";import{randomInt,chance}from"../core/rng.js";import{marketHeat}from"./world-market.js";
export function ensureWorkLifecycle(work){work.lifecycle??={stage:"released",ageWeeks:0,heat:Math.max(20,(work.quality||50)+(work.stars||1)*8),wordOfMouth:50,totalFanGain:0,totalRevenue:0,lastTickWeek:work.completedWeek||state.week};return work.lifecycle}
export function tickWorkLifecycles(){const updates=[];for(const work of state.completedWorks||[]){const l=ensureWorkLifecycle(work);if(l.lastTickWeek===state.week)continue;l.lastTickWeek=state.week;l.ageWeeks+=1;const market=(marketHeat(work.category)-50)/5,quality=(work.quality||50)-55;l.wordOfMouth=Math.max(0,Math.min(100,l.wordOfMouth+Math.round(quality/18)+randomInt(-4,4)));l.heat=Math.max(0,Math.min(100,Math.round(l.heat*.84+l.wordOfMouth*.13+market)));if(l.ageWeeks<=4){const fanGain=Math.max(0,Math.round(l.heat/8)+randomInt(0,5));state.fans+=fanGain;l.totalFanGain+=fanGain;if(work.original){const revenue=Math.max(0,Math.round(l.heat*25*(1+l.wordOfMouth/100)));state.money+=revenue;l.totalRevenue+=revenue}if(chance(Math.min(25,Math.max(2,l.wordOfMouth/5)))&&l.wordOfMouth>=65){state.rep.路人緣=Math.min(1000,(state.rep.路人緣||0)+4);updates.push(`《${work.title}》靠口碑持續發酵`)}}if(l.ageWeeks===1)l.stage="opening";else if(l.ageWeeks<=4)l.stage="theatrical";else if(l.ageWeeks<=12)l.stage="longtail";else l.stage="catalog"}return updates}
