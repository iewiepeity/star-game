// 手機通訊錄只列出玩家實際認識過的人物；訊息也必須來自已建立的關係。
import{NPCS}from"../data/npcs.js";import{state}from"../core/state.js";import{relationshipStance}from"../logic/npc-engine.js";import{romanceStageLabel}from"../logic/romance-engine.js";import{esc}from"../core/utils.js";
import{npcApp}from"./npc.js";
export function peopleApp(){const known=state.knownPeople.filter(id=>NPCS[id]),messages=(state.npcMessages||[]).filter(m=>known.includes(m.npcId)).slice(-20).reverse(),unread=messages.filter(m=>!m.read).length;return`<div class="inside-page"><div class="inside-title"><div><span>CONTACTS</span><h2>手機通訊錄</h2></div><p>${known.length} 位聯絡人・${unread} 則未讀</p></div>${known.length?`<div class="contact-list">${known.map(id=>{const n=NPCS[id],r=state.relationships[id]||{closeness:0,trust:0,hostility:0,romance:"none"},stance=relationshipStance(r).label,romance=(r.hostility||0)<20&&r.romance&&r.romance!=="none"?`・${romanceStageLabel(r.romance)}`:"";return`<button class="contact-card ${(r.hostility||0)>=45?"conflict":""}" data-select-npc="${id}"><div>${n.avatar}<img class="portrait-img" src="${n.head||n.portrait||""}" alt="" loading="lazy" decoding="async" onerror="this.remove()"></div><span><b>${esc(n.name)}</b><small>${esc(n.job)}</small></span><em>${stance}${romance}</em></button>`}).join("")}</div>${messages.length?`<section class="message-inbox"><h3>人物訊息</h3>${messages.map(m=>{const n=NPCS[m.npcId];return`<button data-select-npc="${m.npcId}" class="${m.read?"":"unread"}"><b>${esc(n?.name||"未知人物")}</b><span>${esc(m.text||m.message||m.label||"有一則新訊息")}</span><small>第 ${m.week||state.week} 週</small></button>`}).join("")}</section>`:""}`:`<div class="tablet-empty"><span>♡</span><h3>還沒有認識任何人</h3><p>第一輪沒有免費人脈。安排自由活動或參加工作，讓第一次相遇真的發生。</p><button data-go-free>去安排自由活動</button></div>`}</div>`}
export function peopleHubApp(){const section=state.peopleSection==="profiles"?"profiles":"contacts";return`<div class="people-hub"><nav class="people-hub-tabs" aria-label="人物功能"><button class="${section==="contacts"?"active":""}" data-people-section="contacts"><span>CONTACTS</span><b>訊息與關係</b></button><button class="${section==="profiles"?"active":""}" data-people-section="profiles"><span>PERSONAL FILES</span><b>人物檔案</b></button></nav><div class="people-hub-content">${section==="profiles"?npcApp():peopleApp()}</div></div>`}
