import{OFFICIAL_SOCIAL_POSTS,NPC_SOCIAL_COPY,SOCIAL_POST_TEMPLATES}from"../data/social.js";
import{NPCS}from"../data/npcs.js";
import{state}from"../core/state.js";
import{esc}from"../core/utils.js";
import{publicOpinionLabel,publicOpinionSnapshot}from"../logic/reputation-engine.js";
function postCard(post){const liked=state.likedSocialPosts.includes(post.id),comments=post.comments||[];return `<article class="social-post"><header><i>${post.image?`<img src="${post.image}" alt="" loading="lazy">`:esc(post.avatar||"✦")}</i><div><b>${esc(post.name)}</b><small>${post.badge}・第 ${post.week||state.week} 週</small></div></header><p>${esc(post.text)}</p><footer><button class="${liked?"liked":""}" data-social-like="${post.id}">♡ ${(post.likes||0)+(liked?1:0)}</button><span>回覆 ${comments.length}</span></footer>${comments.length?`<div class="social-comments">${comments.slice(0,3).map(c=>`<p><b>${esc(typeof c==="string"?"網友":c.name)}</b> ${esc(typeof c==="string"?c:c.text)}</p>`).join("")}</div>`:""}</article>`}
function feed(){const own=state.socialPosts.map(p=>({...p,name:state.name,badge:"本人",avatar:state.name.slice(0,1)}));const news=(state.industryNews||[]).slice(0,8).map(n=>({id:`social-${n.id}`,name:"星望娛樂快訊",badge:n.category,avatar:"✦",week:n.week,text:`${n.title}｜${n.body}`,likes:Math.max(30,Math.round((n.heat||50)/2)),comments:["這週娛樂圈好多事。","先卡，等後續。"]}));const npc=state.knownPeople.filter(id=>NPCS[id]&&NPC_SOCIAL_COPY[id]).map((id,i)=>{const n=NPCS[id];return{id:`npc-${id}`,name:n.name,badge:n.job,avatar:n.avatar,image:n.head,text:NPC_SOCIAL_COPY[id],likes:86+i*37+state.week*3,comments:["工作辛苦了！","期待下次更新。"]}});return[...own,...news,...npc,...OFFICIAL_SOCIAL_POSTS]}
export function socialApp(){const estimated=Math.max(12,state.fans+state.fame*4),op=publicOpinionSnapshot();return `<div class="social-page"><header class="social-profile"><i>${esc(state.name.slice(0,1))}</i><div><span>@star_${esc(state.name.toLowerCase().replace(/\s/g,""))||"newcomer"}</span><h2>${esc(state.name)}</h2><p>新人藝人・${publicOpinionLabel()}・品牌信任 ${op.brandTrust}</p></div><dl><div><b>${state.socialPosts.length}</b><small>貼文</small></div><div><b>${estimated}</b><small>追蹤者</small></div><div><b>${op.heat}</b><small>話題熱度</small></div></dl></header><section class="social-compose"><b>分享近況</b><div>${Object.entries(SOCIAL_POST_TEMPLATES).map(([id,t])=>`<button data-social-post="${id}"><i>${t.icon}</i><span><b>${t.label}</b><small>${t.text}</small></span></button>`).join("")}</div>${state.socialNotice?`<p>${esc(state.socialNotice)}</p>`:""}</section><div class="social-feed">${feed().map(postCard).join("")}</div></div>`}
