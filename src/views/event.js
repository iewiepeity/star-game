import{state}from"../core/state.js";
import{esc}from"../core/utils.js";
export function eventView(){const item=state.activeEvent;if(!item)return`<main class="runner"><section class="event-card"><h1>事件處理完成</h1><button id="event-continue" class="main-btn">繼續</button></section></main>`;const e=item.event,choices=e.choices||[];return`<main class="runner"><section class="event-card"><span>${esc(e.kind||"事件")}</span><h1>${esc(e.title||"突發事件")}</h1><p>${esc(e.text||"")}</p>${choices.length?`<div class="decision-list">${choices.map(c=>`<button data-event-choice="${esc(c.id)}"><b>${esc(c.label)}</b>${c.note?`<small>${esc(c.note)}</small>`:""}</button>`).join("")}</div>`:`<button id="event-resolve" class="main-btn">確認</button>`}<button id="event-skip" class="ghost-btn">稍後繼續</button></section></main>`}
