// 畫面層：星望市地圖 App——選擇自由活動地點；地點資料本身在 data/map-locations.js。
import{MAP_LOCATIONS,MAP_CATEGORIES}from"../data/map-locations.js";
import{DAYS}from"../data/calendar.js";
import{state}from"../core/state.js";

export function mapApp(){const selected=state.freeLocations[state.selectedDay],filter=state.mapFilter||"全部",locations=Object.entries(MAP_LOCATIONS).filter(([,l])=>filter==="全部"||l.category===filter);return `<div class="map-page"><div class="map-intro"><div><span>STARWISH CITY・${Object.values(MAP_LOCATIONS).filter(l=>!l.locked).length} 個可探索地點</span><h2>今天想去哪裡？</h2><p>${state.selectedDay!=null?`將為${DAYS[state.selectedDay]}安排自由活動。`:"選擇地點探索星望市。"}</p></div><b>基本交通費 $300</b></div><nav class="map-filters">${MAP_CATEGORIES.map(c=>`<button class="${filter===c?"active":""}" data-map-filter="${c}">${c}</button>`).join("")}</nav><div class="city-map">${locations.map(([id,l])=>`<button class="map-place ${l.locked?"locked":""} ${selected===id?"selected":""}" ${l.locked?"disabled":""} data-map-location="${id}"><i>${l.icon}</i><span><small>${l.area}・${l.category}</small><b>${l.name}</b><em>${l.note}</em><strong>${l.effect}</strong></span>${l.locked?`<u>尚未解鎖</u>`:`<u>選擇地點 →</u>`}</button>`).join("")}</div><aside class="map-tip">選「主動探索」較容易認識人物；選「專注體驗」則能獲得地點的額外能力成長。每一天的目的地會分開保存。</aside></div>`}
