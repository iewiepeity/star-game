import { state } from "../core/state.js";import { esc } from "../core/utils.js";import { NPCS } from "../data/npcs.js";import { careerPhase } from "../logic/career-phases.js";import { publicOpinionLabel } from "../logic/reputation-engine.js";import { annualTentpoleStatus,fameLifeTier } from "../logic/playable-depth-engine.js";
const meter=(label,value,max=100)=>`<div><span>${label}<b>${Math.round(value||0)}</b></span><i><b style="width:${Math.min(100,Math.max(0,((value||0)/max)*100))}%"></b></i></div>`;
export function worldApp(){const phase=careerPhase(),market=state.worldMarket||{},manager=state.managerState,rivals=Object.entries(state.competitors||state.rivals||{}).slice(0,5),careers=Object.entries(state.npcCareers||{}).filter(([id])=>NPCS[id]).sort((a,b)=>(b[1].momentum||0)-(a[1].momentum||0)).slice(0,6),works=[...(state.completedWorks||[])].reverse().slice(0,4),news=(state.industryNews||[]).slice(0,6),living=[...(state.livingWorldFeed||[])].reverse().slice(0,8),signal=[...(state.worldSignalHistory||[])].reverse()[0],advice=state.managerAdvice,tent=annualTentpoleStatus(),fame=fameLifeTier(),memories=[...(state.relationshipMilestones||[])].reverse().slice(0,5),chains=[...(state.eventChains||[])].reverse().slice(0,4);return`<div class="world-page"><header class="world-hero"><div><span>INDUSTRY DESK・第 ${phase.year} 年</span><h2>${esc(phase.label)}</h2><p>${esc(phase.goal)}</p><small>${esc(phase.pressure||"")}</small></div><strong>${esc(market.headline||"市場正在等待本週的新作品")}</strong></header>${tent?`<section class="world-living-feed"><header><b>接下來的大日子</b><span>CALENDAR PRESSURE</span></header><article class="world-row"><span>${tent.delta===0?"本週":"倒數"}</span><b>${esc(tent.title)}</b><small>${esc(tent.text)}</small></article></section>`:""}<section class="world-status-grid"><article><span>玩家狀態</span>${meter("健康",state.health)}${meter("心情",state.mood)}${meter("疲勞",state.fatigue)}${meter("體力",state.stamina)}</article><article><span>公眾視線</span><b>${publicOpinionLabel()}・${esc(fame.label)}</b><p>話題 ${state.rep.話題度||0}・可信度 ${state.rep.可信度||0}・爭議 ${state.rep.爭議度||0}</p><small>${esc(fame.text)} ${signal?esc(signal.text):""}</small></article><article><span>經紀團隊</span><b>${manager?`${esc(manager.name)}・信任 ${manager.trust}`:"尚未簽約"}</b><p>${manager?`默契 ${manager.chemistry}・壓力 ${manager.stress}`:"簽約後，經紀人的判斷與壓力會在這裡同步。"}</p><small>${advice?`${esc(advice.name)}：「${esc(advice.text)}」`:manager?.history?.at(-1)?.title||"目前沒有新的後台動作"}</small></article></section>${living.length?`<section class="world-living-feed"><header><b>這個世界有記得</b><span>LONG-TAIL ECHOES</span></header>${living.map(item=>`<article class="world-row"><span>${esc(item.type||"世界回聲")}・第 ${item.week} 週</span><b>${esc(item.title||"後續")}</b><small>${esc(item.text||"")}</small></article>`).join("")}</section>`:""}<section class="world-columns"><article><header><b>作品履歷，不只是收藏</b><span>WORK LEGACY</span></header>${works.length?works.map(w=>`<div class="world-row"><b>${esc(w.title)}</b><span>${"★".repeat(w.stars||1)}・${esc(w.lifecycle?.status||w.grade||"發行中")}</span><small>${esc((w.legacyTags||[]).join("・")||w.storyLegacy||w.directionLabel||"作品正在累積觀眾")}${w.crew?.length?`・合作：${esc(w.crew.slice(0,2).map(c=>`${c.role}${c.name}`).join("、"))}`:""}</small></div>`).join(""):`<p class="world-empty">完成第一份工作後，合作班底與作品標籤會留在履歷。</p>`}</article><article><header><b>NPC 不會等你點開聯絡人</b><span>CAREER MOVES</span></header>${careers.length?careers.map(([id,c])=>`<div class="world-row"><b>${esc(NPCS[id].name)}</b><span>Lv.${c.level||1}・${esc(c.trend||"穩定")}</span><small>${esc(c.currentProject||c.lastUpdate||"正在自己的道路上工作")}</small></div>`).join(""):`<p class="world-empty">就算你沒去找他們，圈內人物仍會工作、失敗、轉型與休息。</p>`}</article></section>${memories.length||chains.length?`<section class="world-columns"><article><header><b>共同記憶</b><span>RELATIONSHIP MEMORY</span></header>${memories.map(m=>`<div class="world-row"><b>${esc(NPCS[m.npcId]?.name||"某人")}・${esc(m.label)}</b><small>第 ${m.week} 週・${esc(m.text)}</small></div>`).join("")||`<p class="world-empty">重要關係里程碑會留在這裡。</p>`}</article><article><header><b>還沒結束的事</b><span>EVENT CHAINS</span></header>${chains.map(c=>`<div class="world-row"><b>${esc(c.start)}</b><span>${c.done?"已收束":"仍有後續"}</span><small>你當時選擇：${esc(c.label)}</small></div>`).join("")}</article></section>`:""}<section class="world-columns"><article><header><b>競爭與品牌</b><span>BACKSTAGE</span></header>${rivals.length?rivals.map(([id,r])=>`<div class="world-row"><b>${esc(r.name||NPCS[id]?.name||id)}</b><span>${esc(r.trend||r.status||"觀望")}</span><small>聲勢 ${Math.round(r.momentum||r.fame||0)}</small></div>`).join(""):`<p class="world-empty">競爭者正在形成，第三年後同檔作品與獎季會更直接碰撞。</p>`}<div class="brand-strip">${Object.entries(state.brandRelations||{}).slice(0,5).map(([id,v])=>`<span>${esc(id)} <b>${typeof v==="number"?v:v.score||0}</b></span>`).join("")||"品牌仍在觀察新人期表現"}</div></article><article><header><b>本週娛樂圈</b><span>NEWSROOM</span></header>${news.length?news.map(n=>`<div class="world-row"><b>${esc(n.title)}</b><small>${esc(n.body)}</small></div>`).join(""):`<p class="world-empty">世界新聞會隨工作、獎季、NPC 與市場變化生成。</p>`}</article></section></div>`}
