export function migrateV1ToV2(state){const next=structuredClone(state);if(!Array.isArray(next.freeLocations)&&Array.isArray(next.schedule))next.freeLocations=next.schedule.map(id=>id==="free"?(next.mapLocation||null):null);return next}
export function migrateV2ToV3(state){const next=structuredClone(state);if(!next.activeJobs&&next.jobStage&&next.jobStage!=="available")next.activeJobs={J001:{jobId:"J001",stage:next.jobStage,appliedWeek:1,deadlineWeek:next.jobDeadline||3,remainingSessions:next.jobRemaining??3}};next.activeJobs||={};return next}
export function migrateV3ToV4(state){const next=structuredClone(state);Object.assign(next,{saveVersion:4,eventQueue:Array.isArray(next.eventQueue)?next.eventQueue:[],activeEvent:next.activeEvent||null,health:Number.isFinite(next.health)?next.health:100,overworkStrikes:Number.isInteger(next.overworkStrikes)?next.overworkStrikes:0,awardSeasons:next.awardSeasons&&typeof next.awardSeasons==="object"?next.awardSeasons:{},careerRouteHistory:Array.isArray(next.careerRouteHistory)?next.careerRouteHistory:[],rngSeed:Number.isInteger(next.rngSeed)?next.rngSeed:(Date.now()>>>0),rngCursor:Number.isInteger(next.rngCursor)?next.rngCursor:0});return next}
export function migrateSaveState(state,version){let next=structuredClone(state),v=version;if(v<2){next=migrateV1ToV2(next);v=2}if(v<3){next=migrateV2ToV3(next);v=3}if(v<4){next=migrateV3ToV4(next);v=4}return{version:v,state:next}}
