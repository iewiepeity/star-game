import{state}from"../core/state.js";import{JOB_BY_ID}from"../data/jobs.js";import{jobSource}from"../logic/industry.js";import{applyForJob,scheduleJobAudition,signJob,scheduleJobSession,ensureJobState}from"../logic/job-engine.js";import{scheduleSequelSession}from"../logic/sequel-engine.js";import{render}from"../render.js";
function enhanceJobList(){const catalog=document.querySelector(".job-catalog");if(!catalog||catalog.querySelector(".list-tools"))return;const tools=document.createElement("div"),input=document.createElement("input"),select=document.createElement("select");tools.className="list-tools";input.type="search";input.placeholder="搜尋工作、委託方或類型";input.ariaLabel="搜尋工作";input.value=state.jobQuery||"";select.ariaLabel="工作排序";for(const[value,label]of[["deadline","目前順序"],["stars","星等優先"],["title","名稱排序"]]){const option=document.createElement("option");option.value=value;option.textContent=label;select.add(option)}tools.append(input,select);catalog.querySelector("header")?.after(tools);select.value=state.jobSort||"deadline";const apply=()=>{state.jobQuery=input.value.trim().slice(0,200);state.jobSort=select.value;const cards=[...catalog.querySelectorAll("[data-select-job]")];cards.forEach(card=>card.hidden=!card.textContent.toLowerCase().includes(state.jobQuery.toLowerCase()));if(select.value!=="deadline")cards.sort((a,b)=>select.value==="stars"?(b.textContent.match(/★/g)?.length||0)-(a.textContent.match(/★/g)?.length||0):a.textContent.localeCompare(b.textContent,"zh-Hant")).forEach(card=>catalog.append(card))};input.oninput=apply;select.onchange=apply;apply()}
export function bindJobs(){enhanceJobList();document.querySelectorAll("[data-select-job]").forEach(button=>button.onclick=()=>{state.selectedJobId=button.dataset.selectJob;render()});document.querySelectorAll("[data-sequel-schedule]").forEach(button=>button.onclick=()=>{const result=scheduleSequelSession(button.dataset.sequelSchedule);state.notice=result.message;if(result.ok)state.appOpen="planner";render()});document.querySelectorAll("[data-job-action]").forEach(button=>button.onclick=()=>{const id=button.dataset.jobId,action=button.dataset.jobAction,source=action==="apply"?jobSource(JOB_BY_ID[id]):null,record=ensureJobState(id);if(action==="apply"&&applyForJob(id)){record.sourceType=source.type;record.referrerId=source.referrerId||null;record.sourceAgencyId=source.agencyId||null}if(action==="schedule-audition"||action==="retry"){const day=button.dataset.jobDay==null?null:Number(button.dataset.jobDay),result=scheduleJobAudition(id,day);state.notice=result.message;if(result.ok)state.appOpen="planner"}if(action==="sign")signJob(id);if(action==="schedule"){const day=button.dataset.jobDay==null?null:Number(button.dataset.jobDay),result=scheduleJobSession(id,day);state.notice=result.message;if(result.ok)state.appOpen="planner"}record.notice=record.notice||"";render()});document.querySelectorAll("[data-open-cast-npc]").forEach(button=>button.onclick=()=>{state.selectedNpc=button.dataset.openCastNpc;state.peopleSection="profiles";state.appOpen="people";state.npcArtView="bust";render()})}
