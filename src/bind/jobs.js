import{state}from"../core/state.js";
import{JOB_BY_ID}from"../data/jobs.js";
import{jobSource}from"../logic/industry.js";
import{applyForJob,scheduleJobAudition,signJob,scheduleJobSession,ensureJobState}from"../logic/job-engine.js";
import{scheduleSequelSession}from"../logic/sequel-engine.js";
import{render}from"../render.js";
export function bindJobs(){document.querySelectorAll("[data-select-job]").forEach(button=>button.onclick=()=>{state.selectedJobId=button.dataset.selectJob;render()});document.querySelectorAll("[data-sequel-schedule]").forEach(button=>button.onclick=()=>{const result=scheduleSequelSession(button.dataset.sequelSchedule);state.notice=result.message;if(result.ok)state.appOpen="planner";render()});document.querySelectorAll("[data-job-action]").forEach(button=>button.onclick=()=>{const id=button.dataset.jobId,action=button.dataset.jobAction,source=action==="apply"?jobSource(JOB_BY_ID[id]):null,record=ensureJobState(id);if(action==="apply"&&applyForJob(id)){record.sourceType=source.type;record.referrerId=source.referrerId||null;record.sourceAgencyId=source.agencyId||null}if(action==="schedule-audition"||action==="retry"){const day=button.dataset.jobDay==null?null:Number(button.dataset.jobDay),result=scheduleJobAudition(id,day);state.notice=result.message;if(result.ok)state.appOpen="planner"}if(action==="sign")signJob(id);if(action==="schedule"){const day=button.dataset.jobDay==null?null:Number(button.dataset.jobDay),result=scheduleJobSession(id,day);state.notice=result.message;if(result.ok)state.appOpen="planner"}record.notice=record.notice||"";render()});document.querySelectorAll("[data-open-cast-npc]").forEach(button=>button.onclick=()=>{state.selectedNpc=button.dataset.openCastNpc;state.appOpen="npc";state.npcArtView="bust";render()})}
