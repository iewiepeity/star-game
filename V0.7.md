# v0.7 — 系統整合與垂直深化

> 歷史版本紀錄：本文件保留 v0.7 當時的設計與里程碑。現行 runtime／存檔 schema 已升級至 v14；目前功能與架構請以 `README.md`、`ARCHITECTURE.md`、`DEEPENING-1-16.md` 與 `PLAYABLE-DEPTH-1-16.md` 為準。

本版不增加 NPC、事件或通告數量，目標是把既有內容接成可長期維護的完整遊戲循環。

## Runtime 單一來源

- 通告 runtime 只使用 `state.activeJobs`、`scheduledJobIds` 與 `job_session`。v1～v3 的 `jobStage`／`jobRemaining`／`jobDeadline`／`adshoot` 僅在讀檔遷移階段辨識，進入新版 state 後即消失。
- 通告的申請、試鏡、簽約、排程、執行、完成與違約都由 `logic/job-engine.js` 驗證，不依賴 UI 按鈕作為規則防線。
- 人物關係只由 `logic/npc-engine.js` 修改；事件效果只由 `logic/event-engine.js` 套用；作品與獎項只由 `logic/portfolio.js` 建立；結局只由 `logic/career.js` 鎖定快照。

## 事件與人物

事件 DSL 支援週數、資產、知名度、粉絲、疲勞、心情、健康、能力、聲望、NPC 關係、作品、獎項、公司、通告、地點、行程與職涯路線條件。單一事件可同時套用多種效果。

延遲事件到期時不再背景直接結算，而是進入 `eventQueue`，由專用事件畫面逐件呈現。NPC 關係跨越里程碑也會建立人物事件。10 名主要 NPC 各有聊天、吃飯、應援、合作、關心、約會與和解七類專屬相處事件；玩家排入日程後會在當天做選擇，結果保存於相處記憶。友情、信任、隱藏好感與隱藏敵意分開計算；介面只用愛心、心裂、閃電及緩和符號呈現變化，不揭露精確數值。踩中人物底線可進入芥蒂、不合、交惡及宿敵，影響合作、人脈與戀愛，並透過各人物專屬和解事件修復。感情狀態採 `none → interested → ambiguous → dating → committed → engaged → married`，並支援拒絕、分手、復合、地下戀、公開戀情與唯一伴侶限制。

## 作品、獎項與職涯

作品品質使用 `effectiveStat()`，因此服裝能力加成會與試鏡、資格判定一致；同時納入疲勞、心情、健康與作品對應隱藏特質。

三星以上且品質達標的作品只會先登記為年度獎季候選，不會在殺青瞬間頒獎。年度典禮週才進行入圍與得獎判定。

職涯可形成主路線或雙棲路線。結局開始時會建立 immutable snapshot，之後即使 state 再被修改，也不會改寫已產生的五年結局資料。

## 健康與過勞

疲勞過高會造成健康值持續下降並累積過勞警告；住院可以暫時恢復，但反覆過勞仍可能讓健康歸零或累積足夠警告，進入過勞死亡結局。因此死亡不再是不可達的死規則。

## 存檔與 RNG

v0.7 當時存檔版本為 v12；現行版本已升級至 v14。舊版存檔仍由 `core/migrations.js` 逐版遷移，再由 `hydrateState()` 正規化；v14 另外保存週故事、跨週事件鏈、工作班底、試鏡記憶、作品 legacy、人物共同記憶、經紀人介入、年度大型事件、二周目知識與 UI 使用 telemetry。

所有遊戲亂數統一經過 `core/rng.js` 的 seed + cursor。相同 seed 與狀態可重現相同亂數序列，方便測試與 bug 重現。

## PWA 與測試

Service Worker 對 HTML／JS／CSS 採 network-first，素材採 cache-first；離線時回退既有 cache，新版部署則可取得最新程式。

CI 執行：

```text
npm run validate
npm test
npm run simulate
npm run test:e2e
```

E2E 以 Chromium 同時模擬桌機與手機，涵蓋創角、自動存檔、重新整理及離線啟動。內容 validator 另外檢查通告、NPC、地點、事件、能力、隱藏特質、聲望與圖片資產的跨檔引用；`simulate` 另以 10 種玩法 archetype 跑 260 週壓測中後期重複與失衡。
